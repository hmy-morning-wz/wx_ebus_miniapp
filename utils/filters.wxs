var filters = {
  changeFixedTwo: function (str) {
    var num = str || 0
    var yuan = num / 100
    var money = yuan.toFixed(2)
    console.log(money)
    return money
  }
}

module.exports = {
  changeFixedTwo: filters.changeFixedTwo
}